# DEPRECATED — the old synthetic/natural rarity runner has not yet been
# migrated to the canonical shared-data, 87-outcome, fine-cohort design.
#
# It previously referenced the retired leukemia_sweep.yaml, obsolete outcome
# names, and per-grouped/fine OPERA checkpoints.  Running it would therefore
# produce an incoherent analysis.  It now fails closed in the runner.
#
# Current production rarity workflow:
#   1. Run the generated fine-cohort Cox or IPCW sweeps.
#   2. Run opera.run.hierarchical_rarity with
#      opera/configs/hierarchical_rarity.yaml.
#
# A future synthetic-label-scarcity config must be generated from
# experiment_registry.yaml and include a matched comparator before this runner
# is re-enabled.
deprecated:
  replacement_config: opera/configs/hierarchical_rarity.yaml
  reason: legacy_rarity_runner_not_migrated_to_shared_full_panel
