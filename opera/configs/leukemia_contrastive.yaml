# DEPRECATED — retained only to make the old configuration name fail closed.
#
# This file described the retired per-grouped-folder leukemia setup (including
# obsolete cohort mappings and a five-outcome panel).  It must not be used for
# training, evaluation, or DAPT embedding-store construction.
#
# Production replacement:
#   python -m opera.run.contrastive_multicohort \
#       --config-name generated/joint_opera_full_panel
#
# The replacement is generated from experiment_registry.yaml and uses the one
# shared subject-data root, cohort_membership.parquet, global outcome files,
# the canonical 10 grouped cohorts, and all 87 outcomes.
deprecated:
  replacement_config: opera/configs/generated/joint_opera_full_panel.yaml
  replacement_command: >-
    python -m opera.run.contrastive_multicohort
    --config-name generated/joint_opera_full_panel
  reason: retired_per_grouped_folder_leukemia_setup
